run:
  timeout: 3m
  tests: true
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - revive
    - misspell
issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

